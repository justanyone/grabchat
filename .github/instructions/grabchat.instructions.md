# grabchat instructions

Use the grabchat utility to export Claude Code session transcripts into Markdown whenever the user asks for a transcript export, backup, or summary.

Preferred behavior:
- Use the project-local .chat_history directory by default.
- Prefer the repo-root wrapper command when available.
- Keep the output readable and easy to inspect.
