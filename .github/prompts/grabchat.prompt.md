---
mode: agent
---

When the user asks to export or capture a Claude Code session transcript, use the grabchat utility to generate a Markdown export in the current project.

Preferred workflow:
1. Run the grabchat CLI from the repository root.
2. Save the export to .chat_history/ unless the user requests another path.
3. Report the output path clearly.
