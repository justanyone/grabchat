# grabchat slash command

This command installs and uses the grabchat utility from the current repository.

It expects the project to have the grabchat script available at .claude/scripts/grabchat.py or a repo-root wrapper at scripts/grabchat.py.
